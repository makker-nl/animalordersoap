---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    expose: "true"
    app: AnimalOrderService
    provider: fabric8
    version: 1.0.0-SNAPSHOT
    group: org.mycompany
  name: animalorderservice
spec:
  port:
    targetPort: 8080
  to:
    kind: Service
    name: animalorderservice
